TACOS DEVLOG
Ashley Li, Naomi Kurian, Veronika Duvanova, Ashley Li
P05: Le Fin
----------------------------
isabelZ -- 2026-05-14 11:40 
   - cerbot verified domain & fixed nginx (vm works now!) 

naomiK-- 2026-05-13 00:00
WROKING APP
   - Got app to run locally

ashleyL -- 2026-05-12 00:14
JS STYLE
    - Commeting advice of keeping it short and purposeful were the most relevant part of the guide. It's worth putting blank lines despite interrupting code for cleaner comments.
    - Using 'Node.textContent' instead of 'Element.innerHTML' is better in order to prevent security flaws. We should always take the safer route with our website, especially if the command performs the same.
'if (test) {
  // Perform something if test is true
  // …
  return;
}

// Perform something if test is false
// …'
    - I believe that this piece of code suggesting removing the 'else{...}' function is very useful as it removes unecessary code and decreases redundancy.

isabelZ -- 2026-05-12
PYTHON STYLE 
    - The indents, imports, whitespace in expressions, and naming conventions were the most relevant parts of the study guide. 
    - I believe the indentations and trailing commas will be the most useful to other students, helping with organization and readability (regardless of the time completed). 
FILES = [
    'setup.cfg',
    'tox.ini',
    ]
initialize(FILES,
           error=True,
           )
    - The code demonstrated above, demonstrating the trailing commas conventions, will be used when creating different lists of data pulled from the recipes API we are planning on using. User inputs would also be stored in files like these.

isabelZ -- 2026-05-12
DESIGN DOC SUGGESTION SUMMARY
    - Lacked specificity in goals & formatting errors 
    - Overlapping roles that may cause future conflicts in task-arranging 
    - Needed to add a little more complexity to the overall scope 

naomiK -- 2026-05-11
DD REVISIONS
    - Added specified internal deadlines table
    - Fixed component diagram
    - Added non-goals
    - Reviewed feedback

naomiK -- 2026-05-11
PREPARING
* NEW CHANGES
    - Finished setting up https
    - Link works but website not currently being served
    - Finished dd for in-class review

ashleyL -- 2026-05-05 11:21
    disabled root by changing `PermitRootLogin yes` to `PermitRootLogin no`
    and according to Maya and Piazza, `sudo systemctl restart sshd` should be `sudo systemctl restart ssh`

ashleyL -- 2026-05-04 00:30
    Filled form for VMs

naomiK -- 2026-05-01 11:19
STARTING
* NEW CHANGES:
    - Set up tree in repo
